# Compute the n'th fibonacci number iteratively (O(n)).

var a: int;
var b: int;
var n: int;
var t: int;

while 1 do
  read n;
  a = 0;
  b = 1;
  while n do
    t = a;
    a = b;
    b = b + t;
    n = n - 1;
  done
  print a;
done
